# Prototype runtime artefacts
prototype/node_modules/
prototype/.next/
prototype/db/*.db
prototype/db/*.db-*

# Secrets — never committed (provider keys/tokens live in .env.local only)
.env
.env.*
prototype/.env
prototype/.env.*

# Local tool settings
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store
